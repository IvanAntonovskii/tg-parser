# TG parser

Telegram-бот для криптоновостей. В этой версии к парсингу добавлена генерация изображений.

Стек: Python, aiohttp, BeautifulSoup, Pillow, PyTorch, Diffusers, Transformers.

Токен бота задаётся в локальном `.env`, в репозиторий не входит. Пример — `.env.example`.
